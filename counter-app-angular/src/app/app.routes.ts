import { Routes } from '@angular/router';

/*
  app.routes.ts:
  - Definición de rutas de la aplicación usando la API del Router.
  - En este proyecto pequeño no hay rutas extra; la lista puede crecer cuando se agreguen más pantallas.
*/
export const routes: Routes = [];
